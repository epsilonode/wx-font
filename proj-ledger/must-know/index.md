# Must-Know Index

Short rules to read before changing sensitive areas. Read only the pages your task triggers.

Agent-triggered:

- `ledger-maintenance.md` — before changing `proj-ledger/` structure, roadmaps, logs, memories,
  tooling-router files, or ledger protocols.
- `state-and-process-model.md` — before changing the registry format, codepoint assignments,
  WMO mapping rules, generated output boundaries, or font compilation pipeline.

Orchestrator-triggered:

- `tickets-workflow.md` — before creating, assigning, reviewing, or closing temporary XML tickets
  under `proj-ledger/tickets` (only when directly prompted).